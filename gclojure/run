#!/bin/bash
export PATH=$PATH:$(dirname $0)/node_modules/.bin
STEP=${STEP:-stepA_mal}
if [ "${gclojure_MODE}" = "cljs" ]; then
  echo "CLJS not currently supported"
  exit 1
  exec lumo -c $(dirname $0)/src -m mal.${STEP//_/-} "${@}"
else
  exec java -jar $(dirname $0)/target/${STEP}.jar "${@}"
fi
